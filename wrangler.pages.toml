# ============================================================
# ✅ SmartPages Dashboard - Cloudflare Pages Config
# ------------------------------------------------------------
# Diese Version ist 100% kompatibel mit dem neuen Pages-Build-System
# und vermeidet Wrangler-ParseError ("Unterminated inline array")
# ============================================================

name = "smartpages-dashboard"
compatibility_date = "2024-12-20"
compatibility_flags = ["nodejs_compat"]

# ------------------------------------------------------------
# 🔧 Build-Konfiguration
# ------------------------------------------------------------
[build]
command = "npm run build"
cwd = "."

# Cloudflare Pages erwartet dieses Feld zwingend
pages_build_output_dir = "dist"

# ------------------------------------------------------------
# 🌍 Production Environment
# ------------------------------------------------------------
[env.production]
# ⚠️ Inline-Formatierung: keine Zeilenumbrüche im vars-Block!
vars = { ENVIRONMENT = "production", PUBLIC_BASE_URL = "https://profi-marketing-llc.workers.dev" }

# ------------------------------------------------------------
# ℹ️ Hinweis:
# KV-Namespaces, D1-Datenbanken und Service-Bindings
# werden automatisch über das Pages-Dashboard injiziert.
# Eine manuelle Angabe in dieser Datei ist nicht nötig.
# ------------------------------------------------------------
