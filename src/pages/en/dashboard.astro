---
import PageLayout from "~/layouts/PageLayout.astro";

const user = {
  name: "Frank Hüser",
  email: "frank@smartpages.online",
  status: "Business",
  plan: "Trial (7 Days)",
  activeUntil: "2026-01-15",
  lastLogin: "2025-12-21 22:14",
};
---

<PageLayout title="Dashboard – SmartPages" sidebar={true} header={true}>
  <section class="max-w-6xl mx-auto bg-gray-50 border border-gray-200 shadow-lg rounded-3xl p-8 mt-2">
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center">
      <div class="w-full">
        <h2 class="text-2xl font-bold mb-4">Welcome back, {user.name} 👋</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-x-12 gap-y-2 text-sm text-gray-700">
          <p><span class="font-semibold">Name:</span> {user.name}</p>
          <p><span class="font-semibold">Email:</span> {user.email}</p>
          <p><span class="font-semibold">Status:</span> {user.status}</p>
          <p><span class="font-semibold">Plan:</span> {user.plan}</p>
          <p><span class="font-semibold">Active until:</span> {user.activeUntil}</p>
          <p><span class="font-semibold">Last login:</span> {user.lastLogin}</p>
        </div>

        <div class="flex justify-center mt-4">
          <button
            class="px-6 py-2 bg-white text-[#1E2A45] border border-gray-300 rounded-xl shadow-sm 
                   hover:bg-gradient-to-r hover:from-[#F5B400] hover:to-[#E47E00] hover:text-white 
                   transition-all duration-300">
            Edit Data
          </button>
        </div>
      </div>

      <div class="mt-4 md:mt-0 md:ml-6">
        <span class="inline-block px-4 py-1.5 text-sm bg-green-200 text-green-900 rounded-full border border-green-300 font-medium">
          Logged in
        </span>
      </div>
    </div>
  </section>

  <!-- 🧱 Product Cards -->
  <section class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-3 gap-5 p-6 mt-4">
    <a href="/en/smartprofile" class="rounded-2xl p-5 shadow-xl bg-gradient-to-r from-[#4E6BFF] to-[#657AFF] text-white hover:scale-[1.03] hover:shadow-2xl transition-all duration-300">
      <h3 class="text-2xl font-bold mb-2">SmartProfile</h3>
      <p class="text-sm">Create your professional profile in minutes — ideal for freelancers, coaches, and entrepreneurs.</p>
      <span class="mt-3 inline-block bg-white/20 text-xs px-3 py-1 rounded-full">In Development</span>
    </a>

    <a href="/en/smartpage" class="rounded-2xl p-5 shadow-xl bg-gradient-to-r from-[#F5B400] to-[#E47E00] text-white hover:scale-[1.03] hover:shadow-2xl transition-all duration-300">
      <h3 class="text-2xl font-bold mb-2">SmartPage</h3>
      <p class="text-sm">Build stunning, high-converting pages effortlessly — simple, fast, and GDPR compliant.</p>
      <span class="mt-3 inline-block bg-white/20 text-xs px-3 py-1 rounded-full">In Development</span>
    </a>

    <a href="/en/smartdomain" class="rounded-2xl p-5 shadow-xl bg-gradient-to-r from-[#8B5CFF] to-[#A076FF] text-white hover:scale-[1.03] hover:shadow-2xl transition-all duration-300">
      <h3 class="text-2xl font-bold mb-2">SmartDomain</h3>
      <p class="text-sm">Secure your own domain directly in SmartPages — professional, simple, and GDPR compliant.</p>
      <span class="mt-3 inline-block bg-white/20 text-xs px-3 py-1 rounded-full">In Development</span>
    </a>
  </section>
</PageLayout>
