---
import "~/styles/global.css";
import ProductGrid from "~/components/shared/ProductGrid.astro";
import { t } from "~/utils/i18n/i18n";

const lang = "en";

/**
 * 🔹 ProductGrid contents (EN)
 * → texts from i18n
 * → design stays inside ProductGrid
 */
const products = [
  {
    title: t(lang, "smartprofile", "title"),
    label: t(lang, "smartprofile", "badge"),
    text: t(lang, "smartprofile", "teaser"),
    action: "/smartprofile",
  },
  {
    title: t(lang, "smartpage", "title"),
    label: t(lang, "smartpage", "badge"),
    text: t(lang, "smartpage", "teaser"),
    action: "/smartpage",
  },
  {
    title: t(lang, "smartdomain", "title"),
    label: t(lang, "smartdomain", "badge"),
    text: t(lang, "smartdomain", "teaser"),
    action: "/smartdomain",
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{t(lang, "login", "page", "title")}</title>
    <meta
      name="description"
      content={t(lang, "login", "page", "description")}
    />
  </head>

  <body class="bg-white text-gray-800 font-inter pt-24 md:pt-28 overflow-x-hidden">
    <!-- 🧭 Header -->
    <header
      class="bg-[#1E2A45] text-white py-5 flex justify-center items-center shadow-lg fixed top-0 left-0 right-0 z-50"
    >
      <div class="flex items-center gap-5">
        <img
          src="/SmartPages_icon_transparent.png"
          alt="SmartPages Logo"
          class="w-16 h-16"
        />
        <div>
          <h1 class="text-3xl font-extrabold">SmartPages</h1>
          <p class="text-sm md:text-base opacity-90">
            {t(lang, "login", "header", "subtitle")}
          </p>
        </div>
      </div>
    </header>

    <!-- 💡 Main Section -->
    <section
      class="w-full max-w-[1500px] mx-auto grid grid-cols-1 md:grid-cols-2 gap-6 px-8 lg:px-12 mt-8 items-start"
    >
      <!-- Info Card -->
      <div class="bg-white shadow-lg p-8 rounded-2xl border border-gray-100">
        <h2 class="text-2xl font-extrabold text-[#F5B400] mb-5">
          {t(lang, "login", "intro", "headline")}
        </h2>

        <p class="mb-3">{t(lang, "login", "intro", "p1")}</p>
        <p class="mb-3">{t(lang, "login", "intro", "p2")}</p>
        <p>{t(lang, "login", "intro", "p3")}</p>
      </div>

      <!-- Login Form -->
      <div class="bg-[#f6f8fb] shadow-lg p-8 rounded-2xl border border-gray-100">
        <!-- FORM bleibt unverändert -->
        <!-- Dein bestehender Formular-Code kann hier 1:1 bleiben -->
      </div>
    </section>

    <!-- 🧱 ProductGrid -->
    <section class="mt-4 mb-8">
      <div class="w-full max-w-[1400px] mx-auto">
        <ProductGrid {products} compact />
      </div>
    </section>

    <footer class="text-center text-sm text-gray-600 py-3 bg-[#f5f5f5]">
      © 2026 SmartPages · Profi Marketing LLC · Hosted in Europe · GDPR-compliant
    </footer>
  </body>
</html>
