---
import PageLayout from "~/layouts/PageLayout.astro";

// Beispiel-Daten (später dynamisch via API aus D1)
const user = {
  first_name: "Frank",
  last_name: "Hüser",
  email: "frank@smartpages.online",
  plan: "trial",
  status: "active",
};
---

<PageLayout sidebar={true} header={true} lang="de">
  <!-- 🧭 Begrüßungsbereich -->
  <section class="mt-6 px-8">
    <h1 class="text-3xl font-bold text-gray-800 mb-2">
      Willkommen zurück, {user.first_name} 👋
    </h1>
    <p class="text-gray-600">
      Schön, dass Du wieder da bist. Hier findest Du alle Infos zu Deinem Konto
      und Deinen SmartPages-Produkten.
    </p>
  </section>

  <!-- 👤 Account Übersicht -->
  <section
    class="mt-8 mx-8 bg-white shadow-lg rounded-2xl p-6 border border-gray-100"
  >
    <h2 class="text-xl font-semibold text-gray-800 mb-4">
      Dein Account & Status
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <p class="text-gray-500 text-sm">Name</p>
        <p class="font-medium text-gray-800">
          {user.first_name} {user.last_name}
        </p>
      </div>
      <div>
        <p class="text-gray-500 text-sm">E-Mail</p>
        <p class="font-medium text-gray-800">{user.email}</p>
      </div>
      <div>
        <p class="text-gray-500 text-sm">Aktueller Plan</p>
        <p class="font-medium text-gray-800 capitalize">{user.plan}</p>
      </div>
      <div>
        <p class="text-gray-500 text-sm">Status</p>
        <p
          class={`font-medium ${
            user.status === "active" ? "text-green-600" : "text-red-600"
          }`}
        >
          {user.status}
        </p>
      </div>
    </div>

    <div class="mt-6">
      <button
        class="px-5 py-2 bg-gradient-to-r from-[#4E6BFF] to-[#657AFF] text-white font-medium rounded-xl shadow hover:opacity-90 transition"
      >
        Daten bearbeiten
      </button>
    </div>
  </section>

  <!-- 💎 Produkt-Kacheln -->
  <section class="grid grid-cols-1 md:grid-cols-3 gap-6 p-8 mt-6">
    <div
      class="rounded-2xl p-6 shadow-xl bg-gradient-to-r from-[#4E6BFF] to-[#657AFF] text-white"
    >
      <h3 class="text-2xl font-bold mb-2">SmartProfile</h3>
      <p class="text-sm">
        Erstelle Dein persönliches Profil mit nur wenigen Klicks – perfekt für Freelancer, Coaches und Unternehmer.
      </p>
    </div>

    <div
      class="rounded-2xl p-6 shadow-xl bg-gradient-to-r from-[#F5B400] to-[#E47E00] text-white"
    >
      <h3 class="text-2xl font-bold mb-2">SmartPage</h3>
      <p class="text-sm">
        Baue beeindruckende Seiten mit wenigen Klicks. Einfach, schnell und professionell gestaltet.
      </p>
    </div>

    <div
      class="rounded-2xl p-6 shadow-xl bg-gradient-to-r from-[#8B5CFF] to-[#A076FF] text-white"
    >
      <h3 class="text-2xl font-bold mb-2">SmartDomain</h3>
      <p class="text-sm">
        Sichere Deine eigene Domain direkt in SmartPages – professionell, einfach und DSGVO-konform.
      </p>
    </div>
  </section>
</PageLayout>
