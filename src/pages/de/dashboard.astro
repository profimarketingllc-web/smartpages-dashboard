---
import PageLayout from "~/layouts/PageLayout.astro";
import "~/styles/global.css";
---

<PageLayout title="Dashboard – SmartPages" sidebar={true} header={true}>
  <div id="dashboard-wrapper">

    <!-- 🧭 Kundendaten -->
    <section class="max-w-6xl mx-auto bg-gray-50 border border-gray-200 shadow-lg rounded-3xl p-6 mt-0">
      <div class="flex flex-col md:flex-row justify-between items-start md:items-center">
        <div class="w-full">
          <h2 class="text-2xl font-bold mb-3 text-[#1E2A45]">
            Willkommen zurück, <span id="userName">Gastbenutzer</span> 👋
          </h2>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-10 gap-y-2 text-sm text-gray-700">
            <p>
              <span class="font-semibold">Name:</span>
              <span id="full_name" class="text-gray-400">–</span>
            </p>
            <p>
              <span class="font-semibold">Status:</span>
              <span id="status" class="text-gray-400">inactive</span>
            </p>
            <p>
              <span class="font-semibold">Plan:</span>
              <span id="plan" class="text-gray-400">Trial</span>
            </p>
            <p>
              <span class="font-semibold">Aktiv bis:</span>
              <span id="trial_end" class="text-gray-400">–</span>
            </p>
            <p>
              <span class="font-semibold">Letztes Login:</span>
              <span id="last_login" class="text-gray-400">–</span>
            </p>
          </div>

          <div class="flex justify-center mt-4">
            <button id="editUserBtn"
              class="px-6 py-2 text-white rounded-xl shadow-md font-medium bg-gradient-to-r from-[#F5B400] to-[#E47E00] hover:opacity-90 transition-all duration-300">
              Kundendaten bearbeiten
            </button>
          </div>
        </div>

        <div class="mt-3 md:mt-0 md:ml-6">
          <span id="login-status"
            class="inline-block px-4 py-1 text-sm rounded-full border bg-[#C8F3C1] text-[#1E2A45] border-[#B1E6AA] font-medium min-w-[110px] text-center">
            Eingeloggt
          </span>
        </div>
      </div>
    </section>

    <!-- 🧾 Impressumsdaten -->
    <sectio
