---
export const prerender = false;

import PageLayout from "~/layouts/PageLayout.astro";
import DashboardCardWide from "~/components/core/DashboardCardWide.astro";
import CustomerCard from "~/components/solid/CustomerCard.jsx";
import ImprintCard from "~/components/solid/ImprintCard.jsx";
import ProductGrid from "~/components/core/ProductGrid.astro";
import SystemMessage from "~/components/core/SystemMessage.astro";
import EditCustomerModal from "~/components/solid/EditCustomerModal.jsx";
import EditImprintModal from "~/components/solid/EditImprintModal.jsx";
import { ClientOnly } from "astro/runtime/client";

// ✅ Serverseitiger Session-Check
const cookie = Astro.request.headers.get("cookie") || "";
if (!cookie.includes("session=")) {
  return Astro.redirect("/de/login");
}
---

<PageLayout title="SmartPages Dashboard" sidebar={true} header={true} lang="de">
  <main
    class="dashboard-wrapper flex flex-col gap-8 max-w-6xl mx-auto px-4 sm:px-6 mt-4 md:mt-6 mb-16 md:mb-20 transition-all duration-300"
  >
    <!-- 🧭 TOP SECTION: Kundendaten + Systemnachricht -->
    <section class="dashboard-top grid grid-cols-1 xl:grid-cols-[2fr_1fr] gap-6 items-start transition-all duration-300">
      <DashboardCardWide>
        <ClientOnly>
          <CustomerCard lang="de" />
        </ClientOnly>
      </DashboardCardWide>

      <div class="system-message">
        <SystemMessage lang="de" />
      </div>
    </section>

    <!-- 🏢 Impressum -->
    <DashboardCardWide>
      <ClientOnly>
        <ImprintCard lang="de" />
      </ClientOnly>
    </DashboardCardWide>

    <!-- 💼 Produktübersicht -->
    <section class="mt-4 md:mt-6">
      <ProductGrid lang="de" />
    </section>

    <!-- 🧩 Modals -->
    <ClientOnly>
      <EditCustomerModal lang="de" />
      <EditImprintModal lang="de" />
    </ClientOnly>
  </main>
</PageLayout>

<style>
  .dashboard-top {
    align-items: flex-start;
  }

  .system-message {
    display: flex;
    justify-content: flex-end;
    align-items: flex-start;
    width: 100%;
    margin-top: 0.25rem;
  }

  @media (min-width: 1280px) {
    .system-message { padding-right: 0.75rem; }
  }

  @media (max-width: 1279px) {
    .system-message {
      justify-content: center;
      text-align: center;
      margin-top: 1rem;
    }
  }

  @media (max-width: 767px) {
    .dashboard-top {
      flex-direction: column;
      gap: 1.25rem;
      align-items: center;
    }
  }

  main { padding-top: 0.5rem; transition: all 0.3s ease-in-out; }
</style>
