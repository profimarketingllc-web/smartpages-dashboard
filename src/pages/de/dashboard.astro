---
import PageLayout from "~/layouts/PageLayout.astro";
import DashboardCardWide from "~/components/core/DashboardCardWide.astro";
import CustomerCard from "~/components/solid/CustomerCard.jsx";
import ImprintCard from "~/components/solid/ImprintCard.jsx";
import ProductGrid from "~/components/core/ProductGrid.astro";
import SystemMessage from "~/components/core/SystemMessage.astro";
import EditCustomerModal from "~/components/solid/EditCustomerModal.jsx";
import EditImprintModal from "~/components/solid/EditImprintModal.jsx";
---

<PageLayout title="SmartPages Dashboard" sidebar={true} header={true}>
  <main class="dashboard-wrapper flex flex-col gap-8 max-w-6xl mx-auto px-4 sm:px-6 mt-4 md:mt-6 mb-16 md:mb-20 transition-all duration-300">
    
    <!-- 🧭 OBERER BEREICH: Kundendaten + Systemnachricht -->
    <section class="dashboard-top grid grid-cols-1 xl:grid-cols-[2fr_1fr] gap-6 items-start transition-all duration-300">
      <!-- 🪪 Kundendaten -->
      <DashboardCardWide>
        <CustomerCard client:load />
      </DashboardCardWide>

      <!-- 💬 Systemnachricht -->
      <div class="system-message flex justify-center xl:justify-end transition-all duration-300">
        <SystemMessage />
      </div>
    </section>

    <!-- 🏢 IMPRESSUM -->
    <DashboardCardWide>
      <ImprintCard client:load />
    </DashboardCardWide>

    <!-- 💼 PRODUKTGRID -->
    <DashboardCardWide>
      <ProductGrid />
    </DashboardCardWide>

    <!-- 🧩 Modals -->
    <EditCustomerModal client:load />
    <EditImprintModal client:load />
  </main>
</PageLayout>

<script>
  window.addEventListener("DOMContentLoaded", () => {
    console.log("✅ Dashboard scripts aktiv (signalbasiert mit MutationObserver)");

    const attachButtonListeners = () => {
      let found = false;

      // Customer Modal
      const customerBtn = document.querySelector("button[data-signal='open-customer-modal']");
      if (customerBtn && !customerBtn.dataset.bound) {
        customerBtn.dataset.bound = "true";
        found = true;
        customerBtn.addEventListener("click", () => {
          console.log("🟢 Öffne Customer Modal");
          window.dispatchEvent(new CustomEvent("open-customer-modal"));
        });
      }

      // Imprint Modal
      const imprintBtn = document.querySelector("button[data-signal='open-imprint-modal']");
      if (imprintBtn && !imprintBtn.dataset.bound) {
        imprintBtn.dataset.bound = "true";
        found = true;
        imprintBtn.addEventListener("click", () => {
          console.log("🟢 Öffne Imprint Modal");
          window.dispatchEvent(new CustomEvent("open-imprint-modal"));
        });
      }

      if (!found) {
        console.warn("⚠️ Noch keine Buttons gefunden – warte auf Solid Hydration …");
      }
    };

    // Sofort versuchen
    attachButtonListeners();

    // DOM-Änderungen beobachten
    const observer = new MutationObserver(() => attachButtonListeners());
    observer.observe(document.body, { childList: true, subtree: true });

    // Nach 10s beenden (Performance)
    setTimeout(() => {
      observer.disconnect();
      console.log("🧹 MutationObserver deaktiviert (nach 10s)");
    }, 10000);

    // Fallbacks
    setTimeout(attachButtonListeners, 1000);
    setTimeout(attachButtonListeners, 3000);
  });
</script>

<style>
  /* ===============================
     🎯 PREMIUM DASHBOARD LAYOUT FIX
     =============================== */

  .dashboard-top {
    align-items: stretch;
  }

  .system-message {
    align-self: stretch;
    display: flex;
    justify-content: flex-end;
  }

  @media (min-width: 1280px) {
    .dashboard-top {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 2rem;
    }
    .system-message {
      justify-content: flex-end;
    }
  }

  @media (min-width: 768px) and (max-width: 1279px) {
    .dashboard-top {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      align-items: center;
    }
    .system-message {
      order: 2;
      justify-content: center !important;
      text-align: center;
      width: 100%;
      display: flex;
    }
  }

  @media (max-width: 767px) {
    .dashboard-top {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
      align-items: center;
    }
    .system-message {
      order: 2;
      justify-content: center !important;
      text-align: center;
      width: 100%;
    }
  }

  main {
    padding-top: 0.5rem;
    transition: all 0.3s ease-in-out;
  }
</style>
