---
export const prerender = false;

import PageLayout from "~/layouts/PageLayout.astro";
import DashboardCardWide from "~/components/shared/DashboardCardWide.astro";
import CustomerCard from "~/components/dashboard/solid/CustomerCard.jsx";
import ImprintCard from "~/components/dashboard/solid/ImprintCard.jsx";
import PrivacyCard from "~/components/dashboard/solid/PrivacyCard.jsx";
import ProductGrid from "~/components/shared/ProductGrid.astro";
import SystemMessage from "~/components/dashboard/astro/SystemMessage.astro";
import EditCustomerModal from "~/components/dashboard/solid/EditCustomerModal.jsx";
import EditImprintModal from "~/components/dashboard/solid/EditImprintModal.jsx";
import EditPrivacyModal from "~/components/dashboard/solid/EditPrivacyModal.jsx";

// 🔹 SSR Sessionprüfung – nur Cookie prüfen
const cookie = Astro.request.headers.get("cookie") || "";
if (!cookie.includes("session=")) {
  return Astro.redirect("/de/login");
}

// 🔹 Userobjekt aus Middleware (Core Worker)
const user = Astro.locals?.user || null;
---

<PageLayout title="SmartPages Dashboard" sidebar={true} header={true} lang="de">
  <main class="dashboard-wrapper flex flex-col gap-8 max-w-6xl mx-auto px-4 sm:px-6 mt-4 md:mt-6 mb-16 md:mb-20 transition-all duration-300">
    <!-- 🔹 Benutzerinfo + Systemstatus -->
    <section class="dashboard-top grid grid-cols-1 xl:grid-cols-[2fr_1fr] gap-6 items-start">
      <DashboardCardWide>
        <CustomerCard client:only="solid-js" lang="de" />
      </DashboardCardWide>

      <div class="system-message">
        <SystemMessage lang="de" user={user} />
      </div>
    </section>

    <!-- 🧾 Impressum -->
    <DashboardCardWide>
      <ImprintCard client:only="solid-js" lang="de" />
    </DashboardCardWide>

    <!-- 🔒 Datenschutzerklärung -->
    <DashboardCardWide>
      <PrivacyCard client:only="solid-js" lang="de" />
    </DashboardCardWide>

    <!-- 🧩 Produktübersicht -->
    <section class="mt-4 md:mt-6">
      <ProductGrid lang="de" />
    </section>

    <!-- 🧠 Modals -->
    <EditCustomerModal client:only="solid-js" lang="de" />
    <EditImprintModal client:only="solid-js" lang="de" />
    <EditPrivacyModal client:only="solid-js" lang="de" />
  </main>
</PageLayout>

<!-- 🔧 Clientseitige Session-Validierung & Cache -->
<script>
  window.addEventListener("DOMContentLoaded", async () => {
    console.log("🇩🇪 Dashboard geladen — prüfe Session...");

    // 🚫 Mehrfachprüfung verhindern
    if (window.__SESSION_CHECK_DONE__) {
      console.log("⚙️ Sessionprüfung bereits ausgeführt, überspringe erneut.");
      return;
    }
    window.__SESSION_CHECK_DONE__ = true;

    // 🧠 Falls Session bereits im Cache ist → direkt nutzen
    if (window.__SESSION_INFO__) {
      console.log("✅ Verwende gecachte Session:", window.__SESSION_INFO__);
      window.dispatchEvent(
        new CustomEvent("session-info-ready", { detail: window.__SESSION_INFO__ })
      );
      return;
    }

    try {
      const res = await fetch("https://api.smartpages.online/api/session/userinfo", {
        credentials: "include",
      });
      const data = await res.json();

      if (!data.ok || !data.user) {
        console.warn("⚠️ Keine gültige Session erkannt – Redirect zum Login");
        window.location.href = "/de/login";
        return;
      }

      console.log("✅ Session aktiv für:", data.user.email);
      window.__SESSION_INFO__ = data.user;

      // 🔁 Event für Solid-Komponenten senden
      window.dispatchEvent(
        new CustomEvent("session-info-ready", { detail: data.user })
      );
    } catch (e) {
      console.error("❌ Fehler bei der Sessionprüfung:", e);
      window.location.href = "/de/login";
    }
  });
</script>
