---
import PageLayout from "~/layouts/PageLayout.astro";

// rein statisch – keine SSR-Logik
const lang = "de";
---

<PageLayout
  lang={lang}
  title="Login – SmartPages"
  sidebar={false}
  header={true}
  sticky={true}
>
  <section class="w-full max-w-4xl mx-auto grid gap-6 mt-6">

    <!-- 👋 Welcome Card -->
    <section class="dashboard-card bg-white rounded-xl shadow p-6">
      <h2 class="text-xl font-bold mb-2">
        Willkommen bei SmartPages
      </h2>
      <p class="text-gray-600">
        Melde Dich an, um Deine Profile, Domains und Abonnements zu verwalten.
      </p>
    </section>

    <!-- 🔐 Login Card -->
    <section class="dashboard-card bg-white rounded-xl shadow p-6">
      <h3 class="text-lg font-semibold mb-4">
        Login
      </h3>

      <!-- später per Solid ersetzen -->
      <form class="grid gap-4">
        <input
          type="email"
          placeholder="E-Mail-Adresse"
          class="border rounded px-3 py-2 w-full"
        />
        <button
          type="submit"
          class="bg-smart-primary text-white rounded py-2 font-semibold"
        >
          Login-Link senden
        </button>
      </form>
    </section>

    <!-- 🧩 Product Grid Placeholder -->
    <section class="dashboard-card bg-white rounded-xl shadow p-6">
      <h3 class="text-lg font-semibold mb-2">
        Deine Produkte
      </h3>
      <p class="text-gray-500">
        Nach dem Login siehst Du hier Deine SmartProfiles, SmartPages und Domains.
      </p>
    </section>

  </section>
</PageLayout>
