---
/**
 * 🔁 SmartPages Redirect Page (stabiler Login-Flow)
 */
---

<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <title>SmartPages Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>

  <body style="background-color:#0b0d17;color:white;font-family:sans-serif;text-align:center;margin-top:20vh;">
    <h2>🔐 Anmeldung wird überprüft...</h2>

    <script is:inline>
      (async () => {
        const url = new URL(window.location.href);
        const token = url.searchParams.get("token");
        const lang = url.searchParams.get("lang") || "de";

        const message = (txt) => {
          document.body.innerHTML =
            `<p style="font-family:sans-serif;text-align:center;margin-top:20vh;">${txt}</p>`;
        };

        if (!token) return message("❌ Kein Token gefunden.");

        try {
          // 1️⃣ Verify
          const verifyRes = await fetch(
            `https://api.smartpages.online/api/auth/verify?token=${token}&lang=${lang}`
          );
          const verifyData = await verifyRes.json();
          if (!verifyData.ok || !verifyData.redirect)
            return message("⚠️ Token ungültig oder abgelaufen.");

          // 2️⃣ Confirm (setzt Session-Cookie)
          const confirmRes = await fetch(
            `https://api.smartpages.online${verifyData.redirect}`,
            { credentials: "include" }
          );
          const confirmData = await confirmRes.json();

          if (confirmData.ok && confirmData.redirect) {
            // 3️⃣ Redirect ins Dashboard
            window.location.href = confirmData.redirect;
          } else {
            message("⚠️ Fehler beim Bestätigen der Anmeldung.");
          }
        } catch (err) {
          message(`❌ Fehler: ${err.message}`);
        }
      })();
    </script>
  </body>
</html>

