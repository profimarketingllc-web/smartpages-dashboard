---
/**
 * ✅ SmartPages Redirect Page (v2)
 * Führt den Login-Flow sauber clientseitig aus:
 * 1️⃣ Verify → prüft Token
 * 2️⃣ Browser-Redirect → Confirm (setzt Cookie)
 * 3️⃣ Server → leitet weiter zum Dashboard
 */
---
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <title>SmartPages Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>

  <body style="background-color:#0b0d17;color:white;font-family:sans-serif;text-align:center;margin-top:20vh;">
    <h2>🔐 Anmeldung wird überprüft...</h2>

    <script is:inline>
      (async () => {
        const url = new URL(window.location.href);
        const token = url.searchParams.get("token");
        const lang = url.searchParams.get("lang") || "de";

        const showMessage = (txt) => {
          document.body.innerHTML =
            `<p style="font-family:sans-serif;text-align:center;margin-top:20vh;">${txt}</p>`;
        };

        if (!token) {
          showMessage("❌ Kein Token gefunden.");
          return;
        }

        try {
          // 1️⃣ Verify Token über Core Worker
          const verifyRes = await fetch(
            `https://api.smartpages.online/api/auth/verify?token=${token}&lang=${lang}`
          );
          const verifyData = await verifyRes.json();

          if (!verifyData.ok || !verifyData.redirect) {
            showMessage("⚠️ Token ungültig oder abgelaufen.");
            return;
          }

          // 2️⃣ Statt fetch() → echter Redirect (damit Cookie gesetzt wird)
          window.location.href = `https://api.smartpages.online${verifyData.redirect}`;

          // Der Confirm-Endpoint erledigt:
          // - Session in KV speichern
          // - Cookie setzen
          // - Redirect zum Dashboard (/de/dashboard)
        } catch (err) {
          showMessage(`❌ Fehler: ${err.message}`);
        }
      })();
    </script>
  </body>
</html>
