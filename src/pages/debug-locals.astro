---
const { user, lang } = Astro.locals;
---

<h2 style="color: #3b82f6;">🧩 Middleware Locals Debug</h2>

{user ? (
  <>
    <h3>👤 Benutzer</h3>
    <pre>{JSON.stringify(user, null, 2)}</pre>
  </>
) : (
  <p>❌ Kein Benutzer gefunden</p>
)}

{lang ? (
  <>
    <h3>🌍 Sprache</h3>
    <p>{lang}</p>
  </>
) : (
  <p>🌍 Keine Sprache gesetzt</p>
)}

<hr />

<details>
  <summary>📦 Gesamte Locals (optional)</summary>
  <pre>{JSON.stringify(Astro.locals, null, 2)}</pre>
</details>
