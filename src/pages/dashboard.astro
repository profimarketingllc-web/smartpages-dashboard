---
export const prerender = false;

import PageLayout from "~/layouts/PageLayout.astro";
import DashboardCardWide from "~/components/shared/DashboardCardWide.astro";
import CustomerCard from "~/components/pages/dashboard/solid/CustomerCard.jsx";
import ImprintCard from "~/components/pages/dashboard/solid/ImprintCard.jsx";
import PrivacyCard from "~/components/pages/dashboard/solid/PrivacyCard.jsx";
import ProductGrid from "~/components/shared/ProductGrid.astro";
import SystemMessage from "~/components/pages/dashboard/astro/SystemMessage.astro";
import EditCustomerModal from "~/components/pages/dashboard/solid/EditCustomerModal.jsx";
import EditImprintModal from "~/components/pages/dashboard/solid/EditImprintModal.jsx";
import EditPrivacyModal from "~/components/pages/dashboard/solid/EditPrivacyModal.jsx";

import dashboardI18n from "~/utils/i18n/dashboard";

/* ------------------------------------------------------------------
   🌍 Sprache (Middleware optional, Fallback safe)
------------------------------------------------------------------ */
const lang = Astro.locals?.language || "de";

/* ------------------------------------------------------------------
   👤 User optional (KEIN Redirect!)
------------------------------------------------------------------ */
const user = Astro.locals?.user || null;

/* ------------------------------------------------------------------
   🌐 i18n
------------------------------------------------------------------ */
const tDashboard = dashboardI18n[lang];

/* ------------------------------------------------------------------
   🧩 Upsell-Produkte (Dashboard-Version)
   → KEINE Badges
   → Ganze Kachel ist Button (Dummy-Link)
------------------------------------------------------------------ */
const upsellProducts = Object.entries(tDashboard.upsell).map(
  ([key, p]) => ({
    key,
    title: p.title,
    text: p.text,
    action: `/features/${key}`, // Dummy / Feature-Seite
  })
);
---

<PageLayout
  title={tDashboard.page.title}
  sidebar={true}
  header={true}
  lang={lang}
>
  <main
    class="dashboard-wrapper flex flex-col gap-8 max-w-6xl mx-auto px-4 sm:px-6 mt-4 md:mt-6 mb-16 md:mb-20 transition-all duration-300"
  >

    <!-- 🧭 TOP -->
    <section class="dashboard-top grid grid-cols-1 xl:grid-cols-[2fr_1fr] gap-6 items-start">
      <DashboardCardWide>
        <CustomerCard client:only="solid-js" lang={lang} />
      </DashboardCardWide>

      <div class="system-message">
        <SystemMessage lang={lang} user={user} />
      </div>
    </section>

    <!-- 🧾 IMPRINT -->
    <DashboardCardWide>
      <ImprintCard client:only="solid-js" lang={lang} />
    </DashboardCardWide>

    <!-- 🔐 PRIVACY -->
    <DashboardCardWide>
      <PrivacyCard client:only="solid-js" lang={lang} />
    </DashboardCardWide>

    <!-- 🚀 PRODUCT UPSELL GRID -->
    <section class="mt-4 md:mt-6">
      <ProductGrid
        lang={lang}
        upsell={{
          smartprofile: upsellProducts.find(p => p.key === "smartprofile"),
          smartpage: upsellProducts.find(p => p.key === "smartpage"),
          smartdomain: upsellProducts.find(p => p.key === "smartdomain"),
          smartlinks: upsellProducts.find(p => p.key === "smartlinks") || null
        }}
      />
    </section>

    <!-- 🪟 MODALS -->
    <EditCustomerModal client:only="solid-js" lang={lang} />
    <EditImprintModal client:only="solid-js" lang={lang} />
    <EditPrivacyModal client:only="solid-js" lang={lang} />

  </main>
</PageLayout>

<!-- ------------------------------------------------------------------
     ℹ️ OPTIONALER SESSION-INFOPULL (NICHT REDIRECTEN!)
------------------------------------------------------------------- -->
<script>
  window.addEventListener("DOMContentLoaded", async () => {
    try {
      const res = await fetch(
        "https://api.smartpages.online/api/session/userinfo",
        { credentials: "include" }
      );

      const data = await res.json();

      if (data?.ok && data.user) {
        window.__SESSION_INFO__ = data.user;
        window.dispatchEvent(
          new CustomEvent("session-info-ready", { detail: data.user })
        );
      }
    } catch {
      // ❌ bewusst leer
      // Dashboard bleibt sichtbar
    }
  });
</script>
