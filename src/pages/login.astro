<!-- ===================== -->
<!-- PREMIUM MODAL (MIT SPINNER) -->
<!-- ===================== -->
<div
  id="loginModal"
  class="fixed inset-0 hidden items-center justify-center bg-black/50 z-50"
>
  <div
    class="bg-white rounded-2xl shadow-strong p-6 max-w-sm w-full text-center"
  >
    <!-- Spinner -->
    <div
      id="modalSpinner"
      class="mx-auto mb-4 h-10 w-10 rounded-full border-4 border-gray-200 border-t-[#F5B400] animate-spin hidden"
    ></div>

    <!-- Status Icon -->
    <div
      id="modalIcon"
      class="mx-auto mb-3 text-3xl hidden"
    ></div>

    <h3 id="modalTitle" class="text-lg font-bold mb-2"></h3>
    <p id="modalText" class="text-sm mb-5 text-gray-600"></p>

    <button
      id="modalBtn"
      class="px-6 py-2 rounded-lg font-semibold text-white hidden"
    >
      OK
    </button>
  </div>
</div>
