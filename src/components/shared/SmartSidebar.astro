---
import { t } from "~/utils/i18n/i18n";
const { lang = "en" } = Astro.props;

const items = [
  {
    key: "dashboard",
    label: t(lang, "sidebar", "dashboard"),
    icon: "🛫",
    href: "/dashboard",
    type: "system",
  },
  {
    key: "smartprofile",
    label: "SmartProfile",
    icon: "👤",
    href: "/smartprofile",
    type: "product",
    gradient: "bg-white hover:bg-gradient-to-r from-[#5B6CFF] to-[#6F7DFF]",
    glow: "hover:smart-glow-blue",
  },
  {
    key: "smartpage",
    label: "SmartPage",
    icon: "📄",
    href: "/smartpage",
    type: "product",
    gradient: "bg-white hover:bg-gradient-to-r from-[#F5B400] to-[#E47E00]",
    glow: "hover:smart-glow-orange",
  },
  {
    key: "smartdomain",
    label: "SmartDomain",
    icon: "🌐",
    href: "/smartdomain",
    type: "product",
    gradient: "bg-white hover:bg-gradient-to-r from-[#8B5CFF] to-[#A076FF]",
    glow: "hover:smart-glow-purple",
  },
  {
    key: "smartlinks",
    label: "SmartLinks",
    icon: "🔗",
    href: "/smartlinks",
    type: "product",
    gradient: "bg-white hover:bg-gradient-to-r from-[#A8D5BA] to-[#91C7A0]",
    glow: "hover:smart-glow-purple",
  },
];
---

<aside class="fixed top-0 left-0 h-screen w-64 bg-[#1E2A45] flex flex-col z-50">
  <!-- Header -->
  <div class="h-20 flex items-center px-6 border-b border-white/10">
    <span class="text-xl font-extrabold text-[#F5B400]">SmartCenter</span>
  </div>

  <!-- Navigation -->
  <nav class="flex-1 px-4 py-6 space-y-3">
    {items.map(item => (
      <a
        href={item.href}
        class={`
          flex items-center gap-3 px-5 py-3 rounded-2xl text-sm font-medium
          text-[#1E2A45] transition-all duration-300
          ${item.type === "system"
            ? "bg-white hover:system-gradient hover:system-glow"
            : `${item.gradient} ${item.glow}`}
        `}
      >
        <span class="text-lg">{item.icon}</span>
        <span class="text-left">{item.label}</span>
      </a>
    ))}
  </nav>

  <!-- Footer -->
  <div class="px-4 py-4 space-y-3 border-t border-white/10">
    <a
      href="/billing"
      class="flex items-center gap-3 px-5 py-3 rounded-2xl text-sm font-medium
             bg-white text-[#1E2A45]
             hover:system-gradient hover:system-glow transition"
    >
      💳 <span>{t(lang, "sidebar", "billing")}</span>
    </a>

    <a
      href="mailto:support@smartpages.online"
      class="flex items-center gap-3 px-5 py-3 rounded-2xl text-sm font-medium
             bg-white text-[#1E2A45]
             hover:system-gradient hover:system-glow transition"
    >
      💬 <span>{t(lang, "sidebar", "support")}</span>
    </a>
  </div>
</aside>
