---
import sidebar from "~/utils/i18n/sidebar";

const { lang = "en" } = Astro.props;
const t = sidebar[lang];

const systemItems = [
  {
    label: t.nav.dashboard,
    icon: "🛫",
    href: "/dashboard",
  },
];

const productItems = [
  { label: t.nav.smartprofile, icon: "👤", href: "/smartprofile" },
  { label: t.nav.smartpage, icon: "📄", href: "/smartpage" },
  { label: t.nav.smartdomain, icon: "🌐", href: "/smartdomain" },
  { label: t.nav.smartlinks, icon: "🔗", href: "/smartlinks" },
];
---

<aside class="fixed inset-y-0 left-0 w-64 bg-smart-bg-dark text-white flex flex-col z-50">

  <!-- Header -->
  <div class="h-20 flex items-center px-6 border-b border-white/10">
    <span class="text-xl font-extrabold text-smart-accent">
      SmartCenter
    </span>
  </div>

  <!-- Navigation -->
  <nav class="flex-1 px-4 py-6 space-y-6">

    <!-- System -->
    <div class="space-y-2">
      {systemItems.map(item => (
        <a
          href={item.href}
          class="flex items-center gap-3 px-5 py-3 rounded-xl
                 bg-white text-smart-text font-medium
                 hover:system-gradient hover:system-glow transition"
        >
          <span>{item.icon}</span>
          <span>{item.label}</span>
        </a>
      ))}
    </div>

    <!-- Products -->
    <div class="space-y-2">
      {productItems.map(item => (
        <a
          href={item.href}
          class="flex items-center gap-3 px-5 py-3 rounded-xl
                 bg-white text-smart-text font-medium
                 hover:bg-smart-accent/10 transition"
        >
          <span>{item.icon}</span>
          <span>{item.label}</span>
        </a>
      ))}
    </div>

  </nav>

  <!-- Footer -->
  <div class="px-4 py-4 space-y-2 border-t border-white/10">

    <a
      href="/billing"
      class="flex items-center gap-3 px-5 py-3 rounded-xl
             bg-white text-smart-text font-medium
             hover:system-gradient hover:system-glow transition"
    >
      💳 <span>{t.footer.billing}</span>
    </a>

    <a
      href="mailto:support@smartpages.online"
      class="flex items-center gap-3 px-5 py-3 rounded-xl
             bg-white text-smart-text font-medium
             hover:system-gradient hover:system-glow transition"
    >
      💬 <span>{t.footer.support}</span>
    </a>

  </div>
</aside>
