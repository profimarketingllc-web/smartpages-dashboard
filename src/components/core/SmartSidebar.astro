---
const navItems = [
  { label: "Cockpit", icon: "✈️", link: "/de/dashboard" },
  { label: "SmartProfile", icon: "👤", link: "/de/dashboard/smartprofile" },
  { label: "SmartPage", icon: "📄", link: "/de/dashboard/smartpage" },
  { label: "SmartDomain", icon: "🌐", link: "/de/dashboard/smartdomain" },
];
---

<aside
  id="sidebar"
  class="fixed top-0 left-0 h-screen w-64 bg-[#1E2A45] text-white shadow-2xl flex flex-col transition-all duration-300 ease-in-out z-40"
>
  <!-- Header -->
  <div class="flex items-center gap-3 p-5 border-b border-white/10">
    <img src="/favicon.svg" alt="SmartPages Logo" class="w-8 h-8" />
    <span class="text-lg font-semibold tracking-wide">SmartPages</span>
  </div>

  <!-- Navigation -->
  <nav class="flex-1 mt-6 space-y-4 px-3">
    {navItems.map((item) => (
      <a
        href={item.link}
        class="flex items-center gap-3 px-4 py-3 rounded-2xl text-sm font-medium bg-white/10 hover:bg-gradient-to-r hover:from-[#F5B400] hover:to-[#E47E00] hover:text-white shadow-md transition-all duration-300 ease-in-out"
      >
        <span class="text-lg">{item.icon}</span>
        <span>{item.label}</span>
      </a>
    ))}
  </nav>
</aside>
