---
import "~/styles/global.css";
import SmartHeader from "~/components/shared/SmartHeader.astro";
import SmartSidebar from "~/components/shared/SmartSidebar.astro";
import { t, useLang } from "~/utils/i18n/i18n";

const {
  sidebar = true,
  header = true,
  page,            // 👈 NEU: "login" | "smartprofile" | ...
} = Astro.props;

// 🌍 Sprache (Fallback = en)
const lang = useLang("en");

// 🧠 Browser-Titel: Brand → Page
const pageTitle = page
  ? `SmartPages – ${t(lang, page, "page", "title")}`
  : "SmartPages";
---
<html lang={lang}>
  <head>
    <title>{pageTitle}</title>
  </head>

  <body class="min-h-screen flex font-sans text-smart-text bg-[#F7F8FA] antialiased">

    {sidebar && <SmartSidebar />}

    <div class={`flex-1 flex flex-col ${sidebar ? "md:ml-64" : ""}`}>

      {header && (
        <SmartHeader
          lang={lang}
          page={page}     <!-- 🔑 ENTSCHEIDEND -->
        />
      )}

      <main class="flex-1 px-4 py-4 max-w-[1500px] w-full mx-auto">
        <slot />
      </main>

      <footer class="text-center text-sm text-gray-500 py-4 border-t bg-white/80">
        © SmartPages
      </footer>

    </div>
  </body>
</html>
