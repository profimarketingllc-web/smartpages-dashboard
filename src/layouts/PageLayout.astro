---
import "~/styles/global.css";
import SmartHeader from "~/components/shared/SmartHeader.astro";
import SmartSidebar from "~/components/shared/SmartSidebar.astro";

/* =========================================================
   🔤 Sprache – Middleware FIRST, Props SECOND, URL LAST
========================================================= */

const {
  sidebar = true,
  header = true,
  sticky = true,
  headerOffset = "compact",
  lang: propLang,
  title = "SmartPages",
} = Astro.props;

// ✅ 1. Middleware
const middlewareLang = Astro.locals?.language;

// ✅ 2. Prop
const langFromProps = propLang;

// ✅ 3. Fallback URL (nur Notnagel)
const pathLang = Astro.url.pathname.includes("/en/") ? "en" : "de";

// 🎯 FINAL
const lang = middlewareLang || langFromProps || pathLang;

// Layout helpers
const headerOffsetClass = headerOffset === "compact" ? "mt-2 md:mt-3" : "mt-12";
const stickyClass = sticky ? "sticky top-0 z-50" : "";
---

<html lang={lang} class="h-full bg-smart-surface">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>

    <meta name="robots" content="noindex, nofollow" />
    <meta
      name="description"
      content={
        lang === "de"
          ? "SmartPages Dashboard — DSGVO-konform und sicher."
          : "SmartPages Dashboard — GDPR compliant and secure."
      }
    />
    <meta name="theme-color" content="#1E2A45" />
    <link rel="icon" href="/favicon.ico?v=2" />
    <link rel="apple-touch-icon" sizes="192x192" href="/favicon-192.png?v=2" />
  </head>

  <body class="min-h-screen flex font-sans text-smart-text bg-[#F7F8FA] antialiased">
    {
      sidebar ? (
        <>
          <SmartSidebar collapsible />

          <div class="flex-1 flex flex-col transition-all duration-300 bg-[#F7F8FA] md:ml-64">
            {header && (
              <div class={stickyClass}>
                <!-- ✅ LANG EXPLIZIT ÜBERGEBEN -->
                <SmartHeader lang={lang} />
              </div>
            )}

            <main
              class={`flex-1 px-2 sm:px-4 md:px-6 lg:px-8 xl:px-10 py-3 md:py-4 
                ${header ? headerOffsetClass : ""} 
                max-w-[1500px] w-full mx-auto transition-all duration-300`}
            >
              <slot />
            </main>

            <footer class="text-center text-sm text-gray-500 py-4 border-t bg-white/80">
              {lang === "de"
                ? <>© {new Date().getFullYear()} SmartPages · Gehostet in Europa · DSGVO-konform</>
                : <>© {new Date().getFullYear()} SmartPages · Hosted in Europe · GDPR-compliant</>
              }
            </footer>
          </div>
        </>
      ) : (
        <div class="flex-1 flex flex-col min-h-screen bg-white">
          {header && (
            <div class={stickyClass}>
              <SmartHeader lang={lang} />
            </div>
          )}

          <main class={`flex-1 px-4 md:px-8 lg:px-10 py-4 md:py-6 ${header ? headerOffsetClass : ""}`}>
            <slot />
          </main>

          <footer class="text-center text-sm text-gray-500 py-4 border-t bg-white/80">
            {lang === "de"
              ? <>© {new Date().getFullYear()} SmartPages · Gehostet in Europa · DSGVO-konform</>
              : <>© {new Date().getFullYear()} SmartPages · Hosted in Europe · GDPR-compliant</>
            }
          </footer>
        </div>
      )
    }
  </body>
</html>
